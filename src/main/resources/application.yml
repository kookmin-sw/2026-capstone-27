spring:
  application:
    name: shield

  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  # MongoDB (RAG 검색용 — RAG_STUB=false 일 때만 활성화)
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/shield}
      database: ${MONGODB_DATABASE:shield}

  # PostgreSQL
  datasource:
    url: ${DB_URL}?prepareThreshold=0&stringtype=unspecified
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 5

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL_USERNAME}
    password: ${GMAIL_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# JWT
jwt:
  secret: ${JWT_SECRET}
  access-token-expiry: 1800000
  refresh-token-expiry: 1209600000

# Google OAuth
google:
  client-id: ${GOOGLE_CLIENT_ID}
  client-secret: ${GOOGLE_CLIENT_SECRET}
  redirect-uri: ${GOOGLE_REDIRECT_URI}

# Supabase Storage
supabase:
  url: ${SUPABASE_URL}
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY}
  storage:
    bucket: lawyer_documents

# Swagger
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /api-docs

# Groq API (replaces xAI Grok)
groq:
  api-key: ${GROQ_API_KEY}
  base-url: https://api.groq.com/openai
  model:
    chat: llama-3.3-70b-versatile
    brief: llama-3.3-70b-versatile
  timeout:
    connect: 5000
    read-chat: 30000
    read-brief: 60000
    read-classify: 15000
  chat:
    max-history-messages: 20
  classify:
    model: llama-3.3-70b-versatile
    temperature: 0.1
    max-tokens: 512
    context-window-messages: 6

# RAG 검색 설정
rag:
  retrieval:
    stub: ${RAG_STUB:true}

# 온톨로지 설정
ontology:
  slim-json-path: classpath:ontology/legal-ontology-slim.json
  cache-ttl-minutes: 60

# CORS
app:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:5173,http://localhost:5174

server:
  port: 8080
