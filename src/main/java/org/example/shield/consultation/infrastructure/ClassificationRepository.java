package org.example.shield.consultation.infrastructure;

/**
 * 분류 결과 JPA Repository.
 *
 * TODO: extends JpaRepository<Classification, UUID>
 * - findByConsultationId(UUID consultationId): Optional<Classification>
 */
public interface ClassificationRepository {
}
