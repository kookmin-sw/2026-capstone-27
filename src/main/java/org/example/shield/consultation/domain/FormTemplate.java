package org.example.shield.consultation.domain;

/**
 * 폼 템플릿 엔티티 - form_templates 테이블 매핑.
 *
 * TODO: @Entity 구현
 * - id: UUID (PK)
 * - legalField: String
 * - fieldName: String
 * - label: String
 * - fieldOrder: int
 * - required: boolean (default true)
 * - collectMethod: CollectMethod (ENUM: CHATBOT / FORM)
 * - createdAt: LocalDateTime
 */
public class FormTemplate {
}
