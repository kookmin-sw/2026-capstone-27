package org.example.shield.consultation.domain;

/**
 * 폼 템플릿 엔티티 - forms 테이블 매핑.
 *
 * TODO: @Entity 구현 (BaseEntity 상속 안 함)
 * - id: UUID (PK)
 * - legalField: String
 * - fieldName: String
 * - label: String
 * - fieldOrder: int
 * - required: boolean (DEFAULT true)
 * - phase: PhaseType (CHATBOT / EXPERT) - 향후 사용
 * - createdAt: LocalDateTime
 */
public class FormTemplate {
}
