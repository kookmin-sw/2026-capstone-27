package org.example.shield.consultation.infrastructure;

/**
 * 구조화 결과 JPA Repository.
 *
 * TODO: extends JpaRepository<CaseStructure, UUID>
 * - findByConsultationId(UUID consultationId): Optional<CaseStructure>
 */
public interface CaseStructureRepository {
}
